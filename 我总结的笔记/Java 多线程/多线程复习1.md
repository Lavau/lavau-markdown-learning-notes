[TOC]

####1.  线程的 5 种状态：

<img src=".\picture\1-1.png" height="220px"/>

#### 2. 创建线程的 4 种方式：

* 直接实现 `Runnable` 接口，实现 `run()` 方法
* 继承 `Thread` 类，重写 `run()` 方法
* 实现 `Callable` 接口，实现 `call()` 方法
* 使用线程池创建线程

#### 3. 线程的基本 “属性”：

##### wait() 线程等待

##### notify() 线程唤醒

##### yield() 线程礼让

##### sleep() 线程休眠

##### 线程的优先级

#### 4. `synchronized` 

##### 4.1 同步方法

##### 4.2 同步代码块

使用同步代码块时，请注意区分锁的具体对象是谁

#### 5. 多线程安全的 List、Set、Map

#### 6. JUC 包下的 锁、线程协同类

#### 7. 解决线程协同问题的模式：

##### 7.1 生产者与消费者模式

采用一个缓存队列

根据缓存队列的存储情况，判断执行生产者线程还是消费者线程

##### 7.2 监听者模式

采用一个标志位

根据标志位的布尔值，判断执行哪个线程

